import { motion } from "framer-motion"
import React from "react"
import styled from "styled-components"

const SearchStyles = styled(motion.span)``

const Search = () => {
  return <SearchStyles>Search</SearchStyles>
}

export default Search
